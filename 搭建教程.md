注意在搭建完成后要导入数据库的时候要删除之前的数据库在重新填写相同的名字并且导入新的数据库



# 宝藏搜 - 搭建教程

> 全能网盘搜索与自动化管理系统搭建指南

---

## 目录

- [环境要求](#环境要求)
- [安装步骤](#安装步骤)
- [常见问题](#常见问题)
- [功能与接口](#功能与接口)
- [网盘配置](#网盘配置)
- [联系方式](#联系方式)

---

## 环境要求

### 系统要求

| 项目 | 要求 |
|------|------|
| **操作系统** | Windows 7+/Linux(Ubuntu/CentOS)/macOS |
| **Python** | 3.8 及以上版本 |
| **MySQL** | 5.7 或 8.0+ |
| **内存** | 建议 2GB 以上 |
| **硬盘** | 建议 10GB 以上 |

### Python 版本检查

```bash
python --version
# 或
python3 --version
```

如果版本低于 3.8，请先升级 Python。

### MySQL 版本检查

```bash
mysql --version
```

---

## 安装步骤

### 第一步：下载源码

将项目源码上传到服务器：

```bash
# 方式1：git 克隆
git clone <你的仓库地址>
cd baozang

# 方式2：直接上传压缩包并解压
# 上传后解压到任意目录
```

### 第二步：安装依赖

#### Windows 系统

```bash
# 创建虚拟环境（推荐）
python -m venv venv

# 激活虚拟环境
venv\Scripts\activate

# 升级 pip
pip install --upgrade pip

# 安装依赖
pip install -r requirements.txt
```

#### Linux/macOS 系统

```bash
# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 升级 pip
pip install --upgrade pip

# 安装依赖
pip install -r requirements.txt
```

### 第三步：配置环境变量

将项目根目录的 `.env.example` 复制为 `.env`：

```bash
cp .env.example .env
```

编辑 `.env` 文件，填写以下配置：

```ini
# 系统密钥 (用于JWT签名，请设置复杂随机字符串)
SECRET_KEY = your_random_secret_key_here

# MySQL 数据库配置
DB_HOST = localhost
DB_PORT = 3306
DB_DATABASE = ucmao_search
DB_USER = root
DB_PASSWORD = your_mysql_password
DB_CHARSET = utf8mb4

# 管理员账号配置
ADMIN_USERNAME = admin
ADMIN_PASSWORD = your_admin_password
```

### 第四步：初始化数据库

#### 方式1：手动初始化

```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE IF NOT EXISTS ucmao_search DEFAULT CHARACTER SET utf8mb4;

# 退出 MySQL
exit

# 导入表结构
mysql -u root -p ucmao_search < schema.sql
```

#### 方式2：使用脚本初始化

```bash
python init_db.py
```

> 注意：脚本会自动读取 `.env` 配置并创建数据库。

### 第五步：启动服务

```bash
python app.py
```

看到以下输出表示启动成功：

```
 * Running on http://127.0.0.1:5004
 * Running on http://192.168.1.100:5004
```

### 第六步：访问系统

打开浏览器访问：
- 本机：`http://127.0.0.1:5004`
- 局域网：`http://你的IP:5004`

### 第七步：后台配置

1. 访问 `http://127.0.0.1:5004/admin`
2. 使用 `.env` 中配置的管理员账号登录
3. 进入"系统配置"页面
4. 配置网盘凭证（Cookie/Token）
5. 开启"公开聚合接口"（如需对外提供API）

---

## Linux 一键部署

项目提供 `install.sh` 一键部署脚本，支持 Ubuntu/Debian/CentOS/Rocky/AlmaLinux：

```bash
# 赋予执行权限
chmod +x install.sh

# 运行安装脚本
sudo ./install.sh
```

脚本会自动完成：
- 安装 Python3、MySQL、Nginx
- 创建虚拟环境
- 安装项目依赖
- 初始化数据库
- 配置 Systemd 服务（开机自启）
- 配置 Nginx 反向代理

---

## 常见问题

### 安装界面卡在第四步

**问题描述**：数据库初始化时卡住或报错。

**解决方案**：
1. 检查 MySQL 服务是否启动：
   ```bash
   # Windows
   net start mysql
   
   # Linux
   systemctl status mysql
   ```

2. 检查数据库连接信息是否正确（`.env` 文件）

3. 手动创建数据库后重试：
   ```sql
   CREATE DATABASE ucmao_search CHARACTER SET utf8mb4;
   ```

### 报错 500

**问题描述**：页面显示 500 Internal Server Error。

**解决方案**：
1. 查看日志文件 `logs/search_ucmao.log`
2. 常见原因：
   - 数据库连接失败 → 检查 `.env` 配置
   - 缺少依赖 → 重新运行 `pip install -r requirements.txt`
   - 端口被占用 → 修改 `app.py` 中的端口号

### 转存中断

**问题描述**：网盘转存过程中断或失败。

**解决方案**：
1. 检查网盘 Cookie/Token 是否过期
2. 检查网盘容量是否已满
3. 检查网络连接是否稳定
4. 查看日志获取详细错误信息

### 伪静态设置

如需使用域名访问并开启伪静态，Nginx 配置如下：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        proxy_pass http://127.0.0.1:5004;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    
    location /static {
        alias /path/to/baozang/static;
        expires 30d;
    }
}
```

### 局域网无法访问

**问题描述**：其他设备无法访问 `http://192.168.1.100:5004`

**解决方案**：
1. 检查防火墙是否放行 5004 端口：
   ```bash
   # Windows
   netsh advfirewall firewall add rule name="宝藏搜" dir=in action=allow protocol=tcp localport=5004
   
   # Linux
   sudo ufw allow 5004/tcp
   ```

2. 检查网络类型是否为"专用网络"：
   ```powershell
   Get-NetConnectionProfile | Set-NetConnectionProfile -NetworkCategory Private
   ```

3. 检查路由器是否开启 AP 隔离（关闭它）

### 每日更新

系统内置定时任务，每 30 分钟自动清理过期临时分享。无需手动操作。

---

## 功能与接口

### 全网搜

宝藏搜支持多网盘聚合搜索，包括：
- 本地资源库搜索
- 第三方 API 搜索（趣盘搜、狗狗盘搜等）
- 外部数据源搜索

### 微信对话平台

可通过 API 接口对接微信公众号/小程序，实现微信内搜索资源。

### 前端页面代码

前端采用响应式设计，支持：
- PC 端浏览器访问
- 移动端浏览器访问
- 资源封面展示
- 分类浏览

---

## API 开放接口

### 资源搜索接口

**接口地址**：`GET /api`

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| keyword | string | 是 | 搜索关键词 |
| cloud_name | string | 否 | 网盘类型筛选 |
| type | string | 否 | 资源类型 |
| limit | int | 否 | 返回数量，默认100 |
| sort | string | 否 | 排序方式：default/random/asc/desc |

**返回示例**：

```json
{
  "success": true,
  "total": 2,
  "results": [
    {
      "source": "hot",
      "name": "庆余年第二季",
      "share_link": "https://pan.quark.cn/s/xxxx",
      "cloud_name": "夸克网盘"
    }
  ]
}
```

### 资源详情接口

**接口地址**：`POST /api/view-link`

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| url | string | 是 | 原始分享链接 |
| title | string | 否 | 资源标题 |
| netdisk_name | string | 否 | 网盘名称 |

**返回示例**：

```json
{
  "success": true,
  "url": "https://pan.quark.cn/s/xxxx",
  "mode": "copy"
}
```

### 网盘转存分享接口

**接口地址**：`POST /create_share`

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | string | 是 | 资源标题 |
| url | string | 是 | 分享链接 |
| netdisk_name | string | 是 | 网盘名称 |

**返回示例**：

```json
{
  "success": true,
  "message": "分享创建成功"
}
```

---

## 网盘配置

### 夸克网盘

1. 登录 [夸克网盘网页版](https://pan.quark.cn)
2. 按 F12 打开开发者工具
3. 刷新页面，在 Network 中找到第一个请求
4. 复制 Request Headers 中的 Cookie
5. 粘贴到后台"系统配置"中

### 阿里网盘

1. 登录 [阿里云盘网页版](https://www.aliyundrive.com)
2. 使用开发者工具获取 refresh_token
3. 粘贴到后台"系统配置"中

### 百度网盘

1. 登录 [百度网盘网页版](https://pan.baidu.com)
2. 按 F12 打开开发者工具
3. 刷新页面，复制 Cookie
4. 粘贴到后台"系统配置"中

### UC网盘

1. 登录 [UC网盘网页版](https://drive.uc.cn)
2. 按 F12 打开开发者工具
3. 刷新页面，复制 Cookie
4. 粘贴到后台"系统配置"中

### 迅雷云盘

迅雷网盘需要三个参数：
- Refresh Token
- Captcha Sign
- User ID

这三个参数需要同时填写，缺一不可。

---

## 联系方式

如有问题或建议，欢迎通过以下方式联系：

- **项目地址**：<你的仓库地址>
- **问题反馈**：请提交 Issue

---

## 免责声明

本工具仅供技术交流学习，严禁用于任何非法目的。因使用本工具造成的任何账号封禁或法律风险，均与原作者无关。

**宝藏搜** - 让每一份网盘资源都为你创造价值。
