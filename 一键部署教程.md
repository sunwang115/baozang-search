# 宝藏搜 - 一键部署教程

> 使用一键脚本快速部署宝藏搜系统

---

## 目录

- [Linux 系统部署](#linux-系统部署)
- [Windows 系统部署](#windows-系统部署)
- [部署后配置](#部署后配置)
- [常见问题](#常见问题)

---

## Linux 系统部署

### 支持系统

- Ubuntu 18.04+
- Debian 9+
- CentOS 7+
- Rocky Linux 8+
- AlmaLinux 8+

### 环境要求

| 项目 | 要求 |
|------|------|
| Python | 3.8+ |
| MySQL | 5.7 或 8.0+ |
| 内存 | 建议 2GB+ |

### 部署前准备

#### 1. 安装 MySQL 并创建数据库

```bash
# 安装 MySQL（如果未安装）
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y mysql-server

# CentOS/Rocky/AlmaLinux
sudo yum install -y mysql-server
sudo systemctl start mysqld

# 启动 MySQL
sudo systemctl start mysql
sudo systemctl enable mysql

# 登录 MySQL 创建数据库
mysql -u root -p
```

在 MySQL 中执行：
```sql
CREATE DATABASE ucmao_search CHARACTER SET utf8mb4;
exit
```

#### 2. 上传项目文件

```bash
# 使用 scp 上传项目到服务器
scp -r search-ucmao-master root@你的服务器IP:/opt/baozang-search/
```

#### 3. 修改脚本配置

编辑 `install.sh`，修改数据库配置：

```bash
# 配置变量（第20-22行）
DB_NAME="ucmao_search"        # 你的数据库名
DB_USER="root"                # 你的用户名
DB_PASSWORD="你的密码"         # 你的密码（必须修改）
```

### 运行部署脚本

```bash
# 进入项目目录
cd /opt/baozang-search/search-ucmao-master

# 赋予执行权限
chmod +x install.sh

# 运行脚本（必须使用 root 权限）
sudo ./install.sh
```

### 脚本执行流程

```
1. 显示数据库配置指南
2. 检查数据库配置是否已修改
3. 检查 root 权限
4. 检测系统类型
5. 安装系统依赖（Python3、Nginx等）
6. 检查 MySQL 状态
7. 准备项目目录
8. [暂停] 确认项目文件已上传
9. 配置 Python 虚拟环境
10. 创建 .env 配置文件
11. 导入 schema.sql 表结构
12. 创建 Systemd 服务
13. 配置 Nginx
14. 配置防火墙
15. 启动服务
16. 显示完成信息
```

### 部署完成

脚本会显示以下信息：

```
========================================
  宝藏搜部署完成！
========================================

访问地址:
  本机: http://127.0.0.1:5004
  外网: http://你的服务器IP

管理员账号:
  用户名: admin
  密码: admin123

管理后台:
  http://你的服务器IP/admin

常用命令:
  启动: systemctl start baozang-search
  停止: systemctl stop baozang-search
  重启: systemctl restart baozang-search
  状态: systemctl status baozang-search
  日志: journalctl -u baozang-search -f

项目目录: /opt/baozang-search/search-ucmao-master
配置文件: /opt/baozang-search/search-ucmao-master/.env
```

---

## Windows 系统部署

### 支持系统

- Windows 7/8/10/11

### 环境要求

| 项目 | 要求 |
|------|------|
| Python | 3.8+ |
| MySQL | XAMPP MySQL |
| 内存 | 建议 2GB+ |

### 部署前准备

#### 1. 安装 XAMPP 并启动 MySQL

1. 下载并安装 XAMPP：[https://www.apachefriends.org/](https://www.apachefriends.org/)
2. 打开 **XAMPP Control Panel**
3. 点击 **MySQL** 的 **Start** 按钮
4. 确认 MySQL 状态显示为绿色/运行中

#### 2. 创建数据库

打开命令提示符或 XAMPP Shell：

```cmd
cd C:\xampp\mysql\bin
mysql.exe -u root -p
```

在 MySQL 中执行：
```sql
CREATE DATABASE ucmao_search CHARACTER SET utf8mb4;
exit
```

#### 3. 修改脚本配置

编辑 `install-windows.bat`，修改以下配置：

```batch
:: 配置变量（第17-19行）
set "DB_NAME=ucmao_search"        :: 你的数据库名
set "DB_USER=root"                :: 你的用户名
set "DB_PASSWORD=你的密码"         :: 你的密码（必须修改）

:: XAMPP 路径（第24行，如果安装路径不同请修改）
set "XAMPP_MYSQL_PATH=C:\xampp\mysql\bin"
```

### 运行部署脚本

1. 将 `install-windows.bat` 复制到项目根目录（`search-ucmao-master`）
2. 双击运行 `install-windows.bat`

### 脚本执行流程

```
1. 显示数据库配置指南
2. 检查数据库配置是否已修改
3. 检查 Python 环境
4. 检查 XAMPP MySQL 是否运行
5. 检查数据库是否已创建
6. 准备项目目录
7. 配置 Python 虚拟环境
8. 创建 .env 配置文件
9. 导入 schema.sql 表结构
10. 创建启动脚本
11. 显示完成信息
```

### 部署完成

脚本会显示以下信息：

```
========================================
  宝藏搜部署完成！
========================================

访问地址:
  本机: http://127.0.0.1:5004
  局域网: http://你的计算机名:5004

管理员账号:
  用户名: admin
  密码: admin123

管理后台:
  http://127.0.0.1:5004/admin

启动方式:
  方式1: 双击 start.bat （显示命令行窗口）
  方式2: 双击 start.vbs （后台运行，无窗口）

项目目录: C:\Users\你的用户名\baozang-search
配置文件: C:\Users\你的用户名\baozang-search\.env

注意: 请确保 XAMPP MySQL 一直在运行
```

---

## 部署后配置

### 1. 登录管理后台

- 地址：`http://你的IP:5004/admin`
- 默认账号：`admin`
- 默认密码：`admin123`

### 2. 配置网盘凭证

进入"系统配置"页面，配置各网盘的 Cookie/Token：

| 网盘 | 配置项 |
|------|--------|
| 夸克网盘 | Cookie |
| 百度网盘 | Cookie |
| 阿里云盘 | Refresh Token |
| UC网盘 | Cookie |
| 迅雷云盘 | Refresh Token + Captcha Sign + User ID |

### 3. 开启公开API（可选）

如需对外提供搜索接口，开启"公开聚合接口"开关。

### 4. 配置分类（可选）

进入"分类管理"页面，创建资源分类。

---

## 常见问题

### Linux 问题

**Q: 脚本提示 "请使用 root 权限运行此脚本"**

A: 使用 `sudo` 运行脚本：
```bash
sudo ./install.sh
```

**Q: 脚本提示 "MySQL 服务未运行"**

A: 手动启动 MySQL：
```bash
sudo systemctl start mysql
```

**Q: 脚本提示 "数据库不存在"**

A: 先创建数据库：
```bash
mysql -u root -p
CREATE DATABASE ucmao_search CHARACTER SET utf8mb4;
exit
```

**Q: 服务启动失败**

A: 查看日志排查问题：
```bash
journalctl -u baozang-search -f
```

### Windows 问题

**Q: 脚本提示 "未检测到 Python"**

A: 安装 Python 3.8+：[https://www.python.org/downloads/](https://www.python.org/downloads/)

**Q: 脚本提示 "未找到 MySQL 命令"**

A: 修改脚本中的 `XAMPP_MYSQL_PATH` 为你的 XAMPP 安装路径：
```batch
set "XAMPP_MYSQL_PATH=D:\你的路径\xampp\mysql\bin"
```

**Q: 脚本提示 "无法连接到 MySQL"**

A: 检查：
1. XAMPP Control Panel 中 MySQL 是否已启动
2. 数据库配置（用户名/密码）是否正确

**Q: 如何后台运行服务？**

A: 使用 `start.vbs` 启动，或在命令行中：
```cmd
cd %USERPROFILE%\baozang-search
venv\Scripts\pythonw.exe app.py
```

---

## 文件说明

| 文件 | 说明 |
|------|------|
| `install.sh` | Linux 一键部署脚本 |
| `install-windows.bat` | Windows 一键部署脚本 |
| `schema.sql` | 数据库表结构文件 |
| `requirements.txt` | Python 依赖列表 |
| `.env.example` | 环境变量配置示例 |

---

## 注意事项

1. **数据库密码必须修改**：脚本会检查 `DB_PASSWORD` 是否为空，为空则退出
2. **Linux 必须使用 root**：脚本需要 root 权限安装系统依赖
3. **Windows 需要先启动 XAMPP MySQL**：脚本会检查 MySQL 是否运行
4. **项目文件必须包含 schema.sql**：用于初始化数据库表结构
5. **部署后修改默认密码**：建议立即修改管理员默认密码

---

**宝藏搜** - 让每一份网盘资源都为你创造价值。
